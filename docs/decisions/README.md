# Architecture decision records

The full reasoning for every decision below lives in the main `README.md`,
section 3 ("Design decisions, layer by layer") — that is the canonical
source. This folder is reserved for any decision that needs MORE detail than
fits in the main README (e.g. a full RU-estimation worksheet, or a benchmark
comparing hopping-window parameter choices) as the project progresses.

No ADRs needed yet beyond what's in README.md section 3.
