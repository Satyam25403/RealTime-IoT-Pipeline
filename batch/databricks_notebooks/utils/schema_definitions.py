"""
PLANNED — Shared schema definitions (bronze/silver/gold) used across all
three notebooks, so schema drift between stages is caught at code level, not
just at runtime. Defines the "minimum required schema" referenced in the
README's schema evolution decision.
"""
